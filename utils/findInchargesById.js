

var findInchargeById = async function(id) {
  // const devices = await Device.find({_id: id})

  // // devices.map((item, index) => {
  // //   item._id
  // // })

  // //传入deviceId
  // const auths = await Auth.find({ device: id })
  // const 



}